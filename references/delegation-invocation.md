# delegation invocation

Status: planned (U4).

— per-adapter headless delegation harness cloned from the Codex pattern (5-field result schema, background-launch + poll, worktree isolation, gate, rollback, preflight).
